MemberResponse:
  type: object
  properties:
    user_id:
      type: string
      format: uuid
    email:
      type: string
    display_name:
      type: string
      nullable: true
    role:
      type: string
      enum: [owner, manager]
  required:
    - user_id
    - email
    - role

AddOrganizationManagerRequest:
  type: object
  properties:
    user_id:
      type: string
      format: uuid
  required:
    - user_id
